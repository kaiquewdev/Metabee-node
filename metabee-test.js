var vows = require('vows'),
    assert = require('assert'),
    Metabee = require('./metabee').activate;

vows.describe('Metabee web crawler tests').addBatch({
    'Access layer': {
        // Access layter tests
    }
}).run();
